# Slack Send Template

Use this template when posting a concise delivery/update message before sending a file.

```text
[artifact or update name]
branch: [branch]
file: [filename]
size: [size]
sha256: [sha256]

[short install or usage note if needed]
```
